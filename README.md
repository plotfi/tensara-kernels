# Tensara Kernels with Launcher

To build launchers with kernels just run:

`./build_all.sh`

List of implemented kernels is incomplete but this will build the activations, and they are runnable in build/bin.
